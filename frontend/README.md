# Frontend

Placeholder. La Fase 4 —capa delgada en Next.js sobre la API del backend— todavía no
empezó.

El backend vive en [`../backend`](../backend) y se levanta desde la raíz del repositorio
con `docker compose up --build`.
